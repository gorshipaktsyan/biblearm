const StyledSubjectComponents = {
  StyledSubjectTitle: styled(Box)({
    fontSize: "16px",
    marginTop: "10px",
    display: "flex",
    justifyContent: "center",
  }),
  StyledSubject: styled(Box)({
    fontSize: "16px",
    textAlign: "center",
  }),
};

export default StyledSubjectComponents;
